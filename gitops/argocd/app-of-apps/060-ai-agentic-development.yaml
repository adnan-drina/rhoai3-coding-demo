apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 060-ai-agentic-development
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: rhoai3-coding-demo
    demo.rhoai.io/stage: "060"
  annotations:
    argocd.argoproj.io/sync-wave: "70"
    argocd.argoproj.io/manifest-generate-paths: gitops/stages/060-ai-agentic-development
  # No finalizer: deleting this Application orphans resources instead of destroying them.
spec:
  project: rhoai-demo
  source:
    repoURL: https://github.com/adnan-drina/rhoai3-coding-demo.git
    targetRevision: main
    path: gitops/stages/060-ai-agentic-development/base
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideDiff=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/annotations
        - /metadata/labels
    - group: operators.coreos.com
      kind: Subscription
      jsonPointers:
        - /metadata/annotations
        - /spec/startingCSV
    # Developers and Dev Spaces toggle started at runtime. Keep the rest of the
    # DevWorkspace spec GitOps-owned so workspace definitions can be updated.
    - group: workspace.devfile.io
      kind: DevWorkspace
      jsonPointers:
        - /spec/started
    - group: ""
      kind: PersistentVolumeClaim
      jsonPointers:
        - /spec
