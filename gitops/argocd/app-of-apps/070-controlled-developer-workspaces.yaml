apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 070-controlled-developer-workspaces
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: rhoai3-coding-demo
    demo.rhoai.io/stage: "070"
  annotations:
    argocd.argoproj.io/sync-wave: "70"
    argocd.argoproj.io/manifest-generate-paths: gitops/stages/070-controlled-developer-workspaces
  # No finalizer: deleting this Application orphans resources instead of destroying them.
spec:
  project: rhoai-demo
  source:
    repoURL: https://github.com/adnan-drina/rhoai3-coding-demo.git
    targetRevision: main
    path: gitops/stages/070-controlled-developer-workspaces/base
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideDiff=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/annotations
        - /metadata/labels
    - group: operators.coreos.com
      kind: Subscription
      jsonPointers:
        - /metadata/annotations
        - /spec/startingCSV
    # DevWorkspace controllers mutate workspace specs at runtime. Keep this
    # temporary drift ignore until workspace ownership is reworked.
    - group: workspace.devfile.io
      kind: DevWorkspace
      jqPathExpressions:
        - .spec
    - group: ""
      kind: PersistentVolumeClaim
      jsonPointers:
        - /spec
