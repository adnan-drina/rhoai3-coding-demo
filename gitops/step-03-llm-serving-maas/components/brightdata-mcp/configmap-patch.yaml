apiVersion: v1
kind: ConfigMap
metadata:
  name: gen-ai-aa-mcp-servers
  namespace: redhat-ods-applications
data:
  BrightData-Web: |
    {
      "url": "http://brightdata-mcp.coding-assistant.svc:3000/sse",
      "transport": "sse",
      "description": "Browse and search the public web (BrightData MCP). Access web pages, search engines, and extract structured data from websites."
    }
