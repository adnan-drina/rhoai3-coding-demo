apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage 070: the agentic workspace — coolstore-inventory-service on the
# demo/agentic-skills branch (AGENTS.md + .opencode/skills Quarkus
# standards), agent-scale memory for OpenCode multi-step runs. Flip the
# checkout revision to main once the skills branch merges.
labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: rhoai3-coding-demo
      demo.rhoai.io/stage: "060"

resources:
  - agentic-workspace.yaml
