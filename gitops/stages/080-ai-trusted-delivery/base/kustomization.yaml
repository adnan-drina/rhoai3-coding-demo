apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage 080 base setup: trusted-delivery operators only. The Securesign
# instance, Trusted Profile Analyzer, and the pipeline for the Stage 070
# migrated application arrive with the stage implementation (see README
# and BACKLOG "Stage 080").
labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: rhoai3-coding-demo
      demo.rhoai.io/stage: "080"

resources:
  - operators/pipelines-subscription.yaml
  - operators/rhtas-subscription.yaml
  - operators/approve-installplans.yaml
