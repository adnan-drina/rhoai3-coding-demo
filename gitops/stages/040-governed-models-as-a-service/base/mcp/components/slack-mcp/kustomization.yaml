apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Enable by adding to base/kustomization.yaml components: section.
# Requires Secret: slack-mcp-credentials with key SLACK_BOT_TOKEN in coding-assistant namespace.
resources:
  - deployment.yaml
patches:
  - path: configmap-patch.yaml
    target:
      kind: ConfigMap
      name: gen-ai-aa-mcp-servers
