apiVersion: serving.kserve.io/v1alpha2
kind: LLMInferenceService
metadata:
  name: gemma-4-26b-a4b
  namespace: models-as-a-service
  labels:
    app.kubernetes.io/name: gemma-4-26b-a4b
    app.kubernetes.io/component: local-llm
    inference.optimization/acceleratorName: L40S
    kueue.x-k8s.io/queue-name: lq-gpu-reserved-demo
    llm-d.ai/deployment-mode: single-gpu-per-replica
    opendatahub.io/dashboard: "true"
    opendatahub.io/genai-asset: "true"
  annotations:
    argocd.argoproj.io/sync-wave: "16"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    openshift.io/display-name: Google Gemma 4 26B A4B Instruct FP8
    opendatahub.io/model-type: generative
    security.opendatahub.io/enable-auth: "true"
    # Governed GPU quota via the Stage 020 HardwareProfile (Queue-based), so the
    # dashboard shows "GPU Reserved - Demo Team" instead of "Custom". The profile
    # binds lq-gpu-reserved-demo — matching this model's queue-name label above.
    opendatahub.io/hardware-profile-name: gpu-reserved-demo
    opendatahub.io/hardware-profile-namespace: redhat-ods-applications
spec:
  model:
    name: gemma-4-26b-a4b
    uri: oci://registry.redhat.io/rhai/modelcar-gemma-4-26b-a4b-it-fp8-dynamic@sha256:065bbfb0a144a6ec6d5b3936a8153b663695a5fac12e3258f451559382c672f8
  replicas: 1
  router:
    gateway:
      refs:
        - name: maas-default-gateway
          namespace: openshift-ingress
    route: {}
    scheduler:
      # Lean scheduler profile (2026-07-14): prefix-cache scoring picks among
      # REPLICAS - at replicas: 1 it scores nothing, so it is disabled here.
      # NOTE: this operator build stamps the tokenizer + modelcar containers
      # onto the scheduler pod REGARDLESS of the plugin config, so the 30-37
      # GiB modelcar image still lands on a CPU worker (verified live; RFE
      # candidate - container composition should follow the config). Removing
      # them entirely needs a bring-your-own EPP via pool.endpointPickerRef
      # (BACKLOG). Revisit this profile if a model scales beyond one replica.
      config:
        inline:
          apiVersion: inference.networking.x-k8s.io/v1alpha1
          kind: EndpointPickerConfig
          plugins:
            - type: single-profile-handler
            - type: queue-scorer
            - type: max-score-picker
          schedulingProfiles:
            - name: default
              plugins:
                - pluginRef: queue-scorer
                  weight: 2
                - pluginRef: max-score-picker
      pool:
        spec:
          endpointPickerRef:
            failureMode: FailOpen
            group: ""
            kind: Service
            name: gemma-4-26b-a4b-epp-service
            port:
              number: 9002
          selector:
            matchLabels:
              app.kubernetes.io/name: gemma-4-26b-a4b
              app.kubernetes.io/part-of: llminferenceservice
              kserve.io/component: workload
          targetPorts:
            - number: 8000
  template:
    containers:
      - name: main
        # No image override: the RHOAI operator supplies the supported vLLM
        # runtime through the versioned kserve-config-llm-template config.
        command:
          - python
          - -m
          - vllm.entrypoints.openai.api_server
        # Serve flags follow the RedHatAI/gemma-4-26B-A4B-it-FP8-dynamic model
        # card: dedicated gemma4 tool parser (the orchestrator-reliability
        # reason for this model), text-only serving (vision/audio encoders
        # skipped to spend the freed memory on KV cache), 128K window
        # (--max-model-len 131072, matched by every client context limit).
        args:
          - "--served-model-name={{.Name}}"
          - --model=/mnt/models
          - --enable-ssl-refresh
          - --ssl-certfile=/var/run/kserve/tls/tls.crt
          - --ssl-keyfile=/var/run/kserve/tls/tls.key
          - --enable-force-include-usage
          - --disable-uvicorn-access-log
          - --enable-prefix-caching
          - --max-model-len=131072
          - --max-num-batched-tokens=8192
          - --max-num-seqs=8
          - --kv-cache-dtype=fp8
          - --enable-auto-tool-choice
          - --tool-call-parser=gemma4
          - --limit-mm-per-prompt={"image":0,"audio":0}
        ports:
          - containerPort: 8000
            name: http
            protocol: TCP
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTPS
          initialDelaySeconds: 600
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTPS
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 60
        resources:
          requests:
            cpu: "2"
            memory: 16Gi
            # One full L40S per model: time-slicing is disabled in Stage 020,
            # so a 1-GPU request is exclusive-card placement by definition.
            nvidia.com/gpu: "1"
          limits:
            cpu: "4"
            memory: 24Gi
            nvidia.com/gpu: "1"
        volumeMounts:
          - mountPath: /dev/shm
            name: shm
    tolerations:
      - effect: NoSchedule
        key: nvidia.com/gpu
        operator: Exists
    volumes:
      - emptyDir:
          medium: Memory
          sizeLimit: 2Gi
        name: shm
