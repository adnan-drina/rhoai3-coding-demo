apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - local-queue.yaml
  - qwen27b-llminferenceservice.yaml
  - qwen27b-maas-modelref.yaml
  # The 35B coding worker moved to ../optional/qwen35b-workshop (multi-user
  # workshop capacity overlay); default posture is single-model on one GPU.
