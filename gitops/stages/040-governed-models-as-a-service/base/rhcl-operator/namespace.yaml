# Red Hat Connectivity Link — provides Gateway policy CRDs (AuthPolicy, RateLimitPolicy)
# Required for MaaS gateway policy and temporary compatibility rate limiting.
apiVersion: v1
kind: Namespace
metadata:
  name: rhcl-operator
  annotations:
    argocd.argoproj.io/sync-wave: "1"
