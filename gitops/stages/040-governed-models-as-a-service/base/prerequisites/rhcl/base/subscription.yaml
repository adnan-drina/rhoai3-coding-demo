apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhcl-operator
  namespace: openshift-operators
  labels:
    app.kubernetes.io/name: rhcl-operator
    app.kubernetes.io/component: api-gateway-governance
  annotations:
    argocd.argoproj.io/ignore-healthcheck: "true"
    argocd.argoproj.io/sync-wave: "3"
spec:
  channel: stable
  installPlanApproval: Manual
  name: rhcl-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  # v1.3.5 matches the approve-rhcl-installplan guard (the 2026-07-10
  # incident rollback initially landed on this stale 1.3.4 pin and the
  # guard failed until the z-stream step was approved).
  startingCSV: rhcl-operator.v1.3.5
