apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSSubscription
metadata:
  name: devspaces-coding-models
  namespace: models-as-a-service
  labels:
    app.kubernetes.io/name: devspaces-coding-models
    app.kubernetes.io/component: maas-subscription
    demo.rhoai.io/provider: governed-models
  annotations:
    argocd.argoproj.io/sync-wave: "18"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  owner:
    users:
      - system:serviceaccount:wksp-ai-developer:devspace-maas-key-provisioner
  modelRefs:
    # Sized for agentic assistants (Kilo Code in 060, OpenCode in 070): a
    # single agent turn carries system prompt + rules + file context, so the
    # old 100K/h chat-era budget exhausted within a few turns (observed live
    # 2026-07-14 as gateway 429s mid-demo). 1M/h then proved too small for a
    # full stage 070 spec-kit workflow — specify+plan+tasks+implement runs
    # 20-38K input tokens per turn across dozens of turns, and rehearsing
    # twice in an hour hit 429 again (observed 2026-07-21). Local models ride
    # our own GPUs, so the budget demonstrates governance, not cost control.
    - name: qwen3-6-27b
      namespace: models-as-a-service
      tokenRateLimits:
        - limit: 5000000
          window: 1h
    - name: qwen3-6-35b-a3b
      namespace: models-as-a-service
      tokenRateLimits:
        - limit: 5000000
          window: 1h
  priority: 100
  tokenMetadata:
    organizationId: rhoai3-coding-demo
    costCenter: ai-platform
    labels:
      environment: demo
      provider: governed-models
      modelSet: devspaces-coding
---
apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSAuthPolicy
metadata:
  name: devspaces-coding-models
  namespace: models-as-a-service
  labels:
    app.kubernetes.io/name: devspaces-coding-models
    app.kubernetes.io/component: maas-auth-policy
    demo.rhoai.io/provider: governed-models
  annotations:
    argocd.argoproj.io/sync-wave: "18"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  subjects:
    users:
      - system:serviceaccount:wksp-ai-developer:devspace-maas-key-provisioner
  modelRefs:
    - name: qwen3-6-27b
      namespace: models-as-a-service
    - name: qwen3-6-35b-a3b
      namespace: models-as-a-service
  meteringMetadata:
    organizationId: rhoai3-coding-demo
    costCenter: ai-platform
    labels:
      environment: demo
      provider: governed-models
      modelSet: devspaces-coding
