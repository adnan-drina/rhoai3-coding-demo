# Dev-grade PostgreSQL for the migrated service. Migrations off Java EE
# monoliths land on a real datasource (Flyway schema + seed data), so the
# runtime environment ships one: ephemeral storage, rebuilt on every start.
apiVersion: v1
kind: Secret
metadata:
  name: ${{ values.name }}-postgres
  labels:
    app.kubernetes.io/part-of: ${{ values.name }}
type: Opaque
stringData:
  POSTGRESQL_USER: coolstore
  POSTGRESQL_PASSWORD: coolstore
  POSTGRESQL_DATABASE: coolstore
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.name }}-postgres
  labels:
    app.kubernetes.io/part-of: ${{ values.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${{ values.name }}-postgres
  template:
    metadata:
      labels:
        app: ${{ values.name }}-postgres
    spec:
      containers:
        - name: postgresql
          image: registry.redhat.io/rhel9/postgresql-15:latest
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: ${{ values.name }}-postgres
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "coolstore", "-d", "coolstore"]
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: {cpu: 100m, memory: 256Mi}
            limits: {memory: 512Mi}
          volumeMounts:
            - name: data
              mountPath: /var/lib/pgsql/data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: ${{ values.name }}-postgres
  labels:
    app.kubernetes.io/part-of: ${{ values.name }}
spec:
  selector:
    app: ${{ values.name }}-postgres
  ports:
    - port: 5432
      targetPort: 5432
