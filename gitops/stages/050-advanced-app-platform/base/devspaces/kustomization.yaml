apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Dev Spaces component (moved from the former assisted-development stage
# base). OpenShift Dev Spaces with Continue AI code assistant, persona
# workspaces, and MaaS API key provisioning.
# The agentic-coolstore workspace is interim: it is retired when the
# agentic-quarkus-scaffold golden-path template lands (Phase 3 of
# docs/PLAN-advanced-app-platform-restructure.md).
# Ref: https://docs.redhat.com/en/documentation/red_hat_openshift_dev_spaces/

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - wait-for-operator-webhook.yaml
  - checluster.yaml
  - repair-devworkspace-sync-options.yaml
  - maas-api-key-provisioning.yaml
  - vscode-editor-configurations.yaml
  - workspaces.yaml
  - agentic-workspace.yaml
