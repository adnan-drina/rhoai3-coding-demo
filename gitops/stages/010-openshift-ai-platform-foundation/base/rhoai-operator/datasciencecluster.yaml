# RHOAI 3.4 DataScienceCluster — minimal component set matching public quickstart
# Ref: https://docs.redhat.com/en/learn/ai-quickstarts/rh-maas-code-assistant
apiVersion: datasciencecluster.opendatahub.io/v2
kind: DataScienceCluster
metadata:
  name: default-dsc
  annotations:
    argocd.argoproj.io/sync-wave: "10"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  components:
    dashboard:
      managementState: Managed
    workbenches:
      managementState: Managed
      workbenchNamespace: rhods-notebooks
    aipipelines:
      managementState: Removed
      argoWorkflowsControllers:
        managementState: Removed
    kserve:
      managementState: Managed
      # Stage 040 enables Models-as-a-Service after it creates the
      # maas-default-gateway the MaaS controller requires. Keeping the
      # baseline Removed lets the DSC reach Ready within Stage 010.
      modelsAsService:
        managementState: Removed
      nim:
        managementState: Removed
      rawDeploymentServiceConfig: Headless
    modelregistry:
      managementState: Managed
      registriesNamespace: rhoai-model-registries
    llamastackoperator:
      managementState: Managed
    kueue:
      managementState: Removed
    trustyai:
      managementState: Managed
      eval:
        lmeval:
          permitOnline: deny
          permitCodeExecution: deny
