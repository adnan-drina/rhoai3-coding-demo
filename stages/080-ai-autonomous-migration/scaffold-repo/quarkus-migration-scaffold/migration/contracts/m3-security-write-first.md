# M3 security write-first (Architect R-M3.29)

**Status:** binding proving-min  
**Sources:** Architect BIND `E-20260810T230310Z` · S-005 hard-block `t_df9cf31a` (0/3 across dual wall)

## Rules

| ID | Rule |
|----|------|
| **R-M3.29** | Security cards: hard `skill_view` → `references/security-config.md` **and** `extensions/spring-to-quarkus-patterns/references/security-anti-essay.md` before first `security/**` edit; write-first / anti-essay |
| **R-M3.28** | Wall-exit-eval / ballot narrative must credit AD-009 freeze + >300s stream latencies before calling a wall-fit PASS body a sizing defect |
| **R-M3.30** | After soft-K spent hard-block with 0 progress: typed `environmental_provider` + **fix-forward new create** (fresh task id) — never silent reclaim |
| **R-M3.31** | Exit-eval: `overall_ok=false` when `wallish` and checkpoint incomplete (even if compile green) |

See also: `wall-exit-eval.md`, `ad011-skill-extension.md`, `m3-wallfit-jdbc.md` (sizing twin is **not** this failure mode).
