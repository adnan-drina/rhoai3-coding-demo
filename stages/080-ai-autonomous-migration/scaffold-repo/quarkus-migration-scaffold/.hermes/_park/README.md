# `.hermes/_park/` — requeue residue, not a skill

No `SKILL.md`. Not agent-discovered.

| Drawer | What |
|--------|------|
| `mint/` | **Deleted** after K4 (`k4_convert.py`). Do not restore the assembler into kernel. |
| `requeue/` | wall/crash requeue, chaos, workspace restore. Authoring-only until Operator names dest GO (Architect `152610Z`). Not day-one product |

Do not mkdir empty `.hermes/kernel/`. K2 is the REHOST of `.hermes/kernel/pre_tool_call.sh` (measured, not claimed control). K1, K3, and K4 live beside it.

Dest clones **omit** this directory (`scripts/bootstrap-migration-scaffold-v2.sh`).
Chaos matrix never dest. Platform git keeps the requeue tree.

K4 payloads assign `orchestrator` (mint-writer / mint-verifier) and
`implementer` (M3). Do not revive EX-4 four-seat names. Do not dest-apply K4.
