name: m3-implementer
skills:
- check-spec-readiness
- derive-story-oracles
- configure-quarkus-profiles
- form-entity-persistence
- spring-to-quarkus-patterns
- manage-quarkus-extensions
- reference-rh-quarkus-pom
- bootstrap-quarkus-project
description: M3 IMPLEMENT phase bundle (AD-002G attach matrix)
instruction: >-
  AD-002E/F/G: preloaded skills include check-spec-readiness,
  derive-story-oracles, configure-quarkus-profiles, form-entity-persistence,
  spring-to-quarkus-patterns, manage-quarkus-extensions,
  reference-rh-quarkus-pom, bootstrap-quarkus-project. Each → skill_view consult OR typed
  skills_unused:<skill>:<reason> before kanban_complete. Silence invalid.
  Derive exit_criteria via derive-story-oracles (class-legal only; no dual
  correct+wrong). Config stories: configure-quarkus-profiles. Entity/mapping
  stories: form-entity-persistence. Hard invoke /spring-to-quarkus-patterns
  (or skill_view) before first destination edit; then open needed references/*
  (rest / di-config / persistence / testing / security-config /
  transitive-supporting-types / cache-adopt-defer / cdi-service-facade).
  BANK-DEST-INV-HARDINVOKE-1: any missing/absent/DEST_MISS/empty-destination
  conclusion is INVALID unless Reasoning cites refs.destination_inventory
  (path+sha256) or evidence/receipts/destination-inventory/; dependency_wait
  REQUIRES that citation first — do not invent OOS owners. Do not invent outside
  files_in_scope. Tip FREEZE for story bodies unless Architect Class A BIND.
