# CS-5-aligned dispatcher settings — specimen-agnostic
# Official: https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban
# Values = demo-validated defaults + one-line rationale.

kanban:
  auto_decompose: false          # ack / human gate; avoid silent child mint
  # auto_promote_children: false # prefer explicit promote when gated
  # review_dispatch: false       # when review lane must stay human
  # failure_limit / max_in_progress: prefer CLI `kanban dispatch --max N`
  #   over forever wrappers when official coverage suffices (CS-5 #5)
