# AD-001 factory baseline — platform facts measured live (specimen-agnostic)
# Official: https://hermes-agent.nousresearch.com/docs/user-guide/configuration
# Secrets via env substitution only — never commit real keys.

model:
  provider: custom
  default: qwen3-6-27b
  base_url: ${MAAS_API_BASE_URL}
  api_key: ${MAAS_API_KEY}
  context_length: 131072
  max_tokens: 8192

tool_loop_guardrails:
  hard_stop_enabled: true
