{
  "schema": "rhoai3.seat-assignee-profiles/v1",
  "cite": "EX-4 E-20260814T195323Z; hermes profile create --description (Profiles / Blank profile); dispatcher silent-fail on unknown names (Kanban / How the orchestrator behaves)",
  "note": "Product worker identities on the seat. Pillar heads stay Cursor — do not create Lead/Architect/Deputy/Review/Research as Hermes profiles and do not touch .wake/.",
  "profiles": {
    "analyzer": {
      "description": "Examines the legacy Spring application: MTA findings, Boot 2 to 3 derivation, and entry-point inventory. Does not write destination product code."
    },
    "planner": {
      "description": "Plans the migration: partitions stories, derives oracles, and runs Spec Kit specify. Does not implement destination code."
    },
    "implementer": {
      "description": "Implements one M3 story against the destination Quarkus tree. Writes only files_writable; completes from typed body exit_criteria."
    },
    "validator": {
      "description": "Checks the destination: domain-parity gates, release readiness, and close-out verdicts. Does not expand story scope."
    }
  },
  "phases": {
    "M1": "analyzer",
    "M2": "planner",
    "M2a": "planner",
    "M2b": "planner",
    "M3": "implementer",
    "M4": "validator",
    "M5": "validator",
    "factory": "validator"
  }
}
