{
  "ex": "EX-5",
  "cite": [
    "Architect E-20260814T194941Z overlays (not a 64-script retirement)",
    "Deputy E-20260814T195323Z done-test",
    "hermes-managed-scope Security: User-Defined Deny Rules, File Write Safety, Container Isolation"
  ],
  "retires_nothing": true,
  "layer1_approvals_deny": [
    "git push --force*",
    "*curl*|*sh*",
    "dd if=* of=/dev/*",
    "rm -rf /",
    "rm -rf /*"
  ],
  "layer2_write_safe_root_env": "HERMES_WRITE_SAFE_ROOT",
  "layer2_write_safe_root_value": "${PROJECT_DIR}",
  "layer2_residual": "HKN-12: native fence returns rc 0 on a deny-prefix path inside SAFE_ROOT. In-repo B-S2 stays the EX-3 write-set hook. B-2 is path-bearing spawn-env HERMES_KANBAN_FILES_WRITABLE (Architect E-20260817T091919Z / v24 7e93fb41): dest path + published [], including TASK-set-unresolved. Dest write-set JSON is cache not policy (Architect 35099226). Tool-name matcher is invocation-only (terminal is defence-in-depth, not a hole-1/2 fix).",
  "layer3_terminal_backend": "local",
  "layer3_reason": "The Dev Spaces workspace pod is the isolation from the cluster node. Nested docker/singularity/modal/daytona/vercel_sandbox are not provisioned (no docker.sock in GitOps). Official container backends skip dangerous-command checks, which would weaken layer 1.",
  "forbidden_terminal_backends": [
    "docker",
    "singularity",
    "modal",
    "daytona",
    "vercel_sandbox"
  ]
}
