# Skill-directory red with green skill_view

Mandated `skill_view` lines and mandated kernel/native argv are green
(omitted rc). `assert-m2-story-headings.py` `[exit 1]` lives under
`plan-migration-partition/scripts/` — a different script. Audit must
**PASS**. A skill step does not own every red in that skill's directory.
