# dest-8 M2 bypass (Operator 123401ZO §4)

Hand-authored `spec.md`, typed `partition.json`, **no** `tasks.md`.
`assert-m2-speckit-conformance.py` must exit 1 (`M2_SPECKIT_BYPASS`).

```bash
python3 .hermes/skills/sdd/check-spec-readiness/scripts/assert-m2-speckit-conformance.py \
  .hermes/skills/sdd/check-spec-readiness/fixtures/m2-speckit-bypass-dest8
```
